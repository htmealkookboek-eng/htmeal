# Python runtime requirements for Render deployment
# Keep a web server entrypoint available even if Render falls back to its default command.
gunicorn==23.0.0
supabase>=2.15.0,<3.0.0
psycopg2-binary>=2.9.9,<3.0.0
