{
  "name": "htmeal",
  "private": true,
  "version": "1.0.0",
  "description": "Local frontend dependencies for the cookbook globe experience",
  "dependencies": {
    "@supabase/ssr": "^0.12.0",
    "@supabase/supabase-js": "^2.110.2",
    "d3": "^7.9.0",
    "topojson-client": "^3.1.0"
  },
  "scripts": {
    "build:static": "python scripts/build_for_gh_pages.py",
    "build:gh-pages": "npm run build:static"
  }
}
